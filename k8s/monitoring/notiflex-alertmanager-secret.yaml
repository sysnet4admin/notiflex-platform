apiVersion: v1
kind: Secret
metadata:
  name: notiflex-alertmanager-slack
  namespace: notiflex
type: Opaque
stringData:
  # 실제 Slack Incoming Webhook URL로 교체하세요.
  webhook-url: "https://hooks.slack.com/services/REPLACE_ME"
