apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: enterprise
spec:
  selector:
    app: notiflex-api
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: notiflex-api-preview
  namespace: enterprise
spec:
  selector:
    app: notiflex-api
  ports:
  - port: 80
    targetPort: 8080
