apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: notiflex-secrets
  namespace: enterprise
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/project-75fce205-dfa5-4975-a56/secrets/valkey-password/versions/latest"
        path: "valkey-password"
